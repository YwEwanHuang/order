# 蔓蔓点菜 审核提交材料

## 版本备注

```
【蔓蔓点菜 v1.0.0】

本版本为蔓蔓点菜工具的首个正式发布版本，主要面向家庭场景，让指定用户（2人）通过微信小程序记录每日晚餐菜品。

【主要功能】
1. 菜品浏览：按"热菜/凉菜/汤/主食"分类展示菜品，含占位图或用户上传的真实图片
2. 选菜：用户可勾选多道菜组成当天晚餐，支持输入备注（≤200字）
3. 保存：选完后可保存到云端，同一日期可覆盖更新
4. 修改：保存后可重新进入选菜页修改
5. 清空：home 页支持一键清空当天记录
6. 历史查看：home 页支持切换日期查看过去任意一天的记录
7. 菜品库管理：支持菜品的新增、编辑（改名/换分类/换图）、删除、启停、排序
8. 图片上传：支持从相册或相机选图，自动压缩后上传至云存储

【技术说明】
- 前端：微信小程序原生 TypeScript + wx.cloud.callContainer 访问后端
- 后端：Express + MySQL，部署于微信云托管
- 数据存储：菜品信息、用户选菜记录均存储于云托管内置 MySQL 中
- 图片存储：菜品图片存放于微信云开发存储

【无敏感权限】
- 不申请用户手机号、地理位置、通讯录等敏感权限
- 不收集用户身份信息（openid 仅用于审计显示，不做权限判断）
- 无登录注册流程，打开即可使用

【无第三方插件/广告】
- 内部实现，无第三方 SDK 集成
- 无广告、无推广、无付费功能
```

## 测试说明

```
【功能测试】

1. 菜品浏览测试
   - 首页（home）会自动加载并展示当日选菜记录
   - 点击日期栏可切换至任意过去日期，查看历史记录
   - 点击右上角"菜品库"标签，进入菜品库页面
   - 菜品库页面左侧为分类栏（热菜/凉菜/汤/主食），右侧为该分类菜品列表
   - 点击左侧分类可滚动到对应分类区域

2. 选菜测试
   - 首页有记录时点击"改一下"，无记录时点击"选晚餐"，进入选菜页
   - 选菜页布局与菜品库一致（侧栏 + 列表）
   - 每道菜品卡片右下角有"+"/"✓"切换按钮，点击切换选中状态
   - 已选菜品数量在底部"已选 N 道"显示
   - 备注输入框支持最多 200 字符
   - 点击底部"保存"按钮保存当日选菜
   - 选中状态可正确保存并显示在首页

3. 修改/清空测试
   - 选菜页点击"保存"后自动返回首页
   - 首页有记录时点击"清空"按钮，弹确认框，确认后清空记录
   - 清空后首页显示"今晚还没选"

4. 历史查看测试
   - 在首页日期选择器选择过去某一天
   - 过去日期不会显示"选晚餐"/"改一下"按钮（只读）
   - 无记录的过去日期显示"当天无记录"
   - 有记录的过去日期正确显示菜品名称

5. 菜品库测试
   - 长按任意菜品卡片，弹出"调整排序"对话框
   - 对话框内可通过 ↑/↓ 按钮调整该分类菜品顺序
   - 点击"保存"后顺序生效，回到列表后顺序正确
   - 点击"+ 新增菜品"或菜品右侧"编辑"，弹出编辑对话框
   - 编辑对话框内可输入菜名（1-16字）、选择分类、选择/更换/移除图片
   - 选择图片时自动从相册/相机选取，系统自动上传
   - 点击菜品右侧"删除"，弹确认框，确认后菜品删除

【兼容性测试】
- 测试机型：iOS 15+、Android 10+
- 测试基础库版本：2.20.1 及以上
- 测试屏幕：iPhone SE（375pt）至 iPad（1024pt）

【测试数据初始化】
- 首次启动时系统自动创建 10 道初始菜品
- 测试时无需预置数据，直接进入即可看到全部菜品
```